import { makeStyles } from "@material-ui/core/styles";

const useStyle = makeStyles({ 
    friend: { color: "#1338BE" },
    hai: { color: "#FF69B4" },
    bigContainer: {minHeight: "100vh"}
});

export default useStyle